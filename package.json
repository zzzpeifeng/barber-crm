{
  "name": "barber-crm-monorepo",
  "private": true,
  "scripts": {
    "dev:backend": "pnpm --filter backend start:dev",
    "dev:h5": "pnpm --filter h5-merchant dev",
    "dev:admin": "pnpm --filter admin-web dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "^5.1.6"
  }
}
